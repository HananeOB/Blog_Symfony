{% extends 'base2.html.twig' %}

{% block title %}Articles{% endblock %}

{% block body %}

    
  <div class="container-fluid mx-auto">
  <div class="row  mx-auto">
    <div class="col-md-8 card mb-4  mt-3  mx-auto">
      <div class="card-body  mx-auto">
        {# <h1>{% block title %} {{ object.title }} {% endblock title %}</h1> #}
        <p class=" text-muted">{{ article.user.email }} | {{ article.dateCreation ? article.dateCreation|date('d-m-Y') : '' }}  | 
                            {% if article.commentaires | length ==0 %}<a href= "{{ path('commentaires_index') }}"> no comment </a>
                            {% else %} <a href= "{{ path('commentaires_index') }}">  {{article.commentaires | length }} comments</a>
                            {% endif %} </p>
        <p class="card-text ">{{article.contenu }}</p>
        </br>
        <img src="{{ asset('uploads/brochures/'~ article.image ) }}" class="img-fluid">
      </div>
    </div>
    
  </div>
</div>

{% if app.user.email is defined %}
                    <div class="container">
                        <div class="row">
                            <div class="col text-center">
                                <a class="btn btn-primary" href="{{ path('articles_edit', {'id': article.id}) }}">Modifier l'article</a>
                                {{ include('articles/_delete_form.html.twig') }}

                            </div>
                        </div>
                    </div>



{% endif %}

<div class="container">
                        <div class="container">
                            <div class="col text-center">
                                <a href="{{ path('articles_index') }}"><span class="tm-nav-deco"></span>Retour à la liste des articles</a>
                            </div>
                        </div>
                    </div>


{% endblock %}
